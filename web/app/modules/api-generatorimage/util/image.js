'use strict';

const fs = require('fs');
const gm = require('gm').subClass({imageMagick: true});


module.exports = function(server, options){
    return {
        generatorImage: function(emailData, callback){

        }
    }
}
